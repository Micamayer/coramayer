@import "colores";
@import "generales";

.comoComprar{
	display: flex;
	flex-direction: row;
	justify-content: center;
	text-aling: $centro;
	font-style: $tipoC;
	font-weight: $pesoFC;
	color: $rojo;
	font-size: $marginT;
	margin:($marginT*2)+10;
	padding:($PaddingT/2)-15;
}